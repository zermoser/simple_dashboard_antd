import styled from "styled-components";
import { theme } from "../../../theme";

export const TableCard = styled.div`
  border-radius: 10px;
  padding: 1em 1em;
  width: 100%;
  overflow-x: scroll;
`;

export const LayoutPageParameter = styled.div`
  background: ${theme.COLORS.WHITE_1};
  border-radius: 10px;
  padding: 1em 2em;
  margin-bottom: 3em;
`;
